<h1>Rewards Shop</h1>

<% @rewards.each do |reward| %>
    <div class="reward">
      <%= link_to reward.name, reward_path(reward) %>: <%= reward.value %>

      <%= button_to "Purchase", user_user_rewards_path, method: "get" %>
    </div>
<% end %>

  <%= render 'layouts/navbar' %>
<h1> Admin Users Index </h1>

<ul>
  <% @users.each do |user| %>
    <li><%= user.id %> <%= link_to user.username, admin_user_path(user) %>: <%= user.total_points %> points</li>
    <% end %>
</ul>
